server:
  port: 8082

spring:
  application:
    name: resource-server

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9001

logging:
  level:
    org.springframework.security: DEBUG
